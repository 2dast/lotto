# 예측 조건 (2안 — 1안과 반대 방향 베팅)

한 줄에 조건 하나씩 적습니다. `#`으로 시작하면 주석(무시)입니다.
조건을 추가/삭제/수정할 때는 이 줄들만 건드리면 됩니다 — `predict.py`가 매번 새로 읽어서 해석합니다.

이 파일은 RULES.md(1안)와 상관관계를 최대한 낮추기 위한 "반대 취향" 버전입니다:
- 1안은 최근 자주 나온 번호를 우대(`freq_mode hot`), 2안은 반대로 최근 안 나온 번호를 우대(`freq_mode cold`).
- 1안은 40~45 구간을 최소 1개 강제, 2안은 반대로 40~45 구간을 아예 배제.
- 1안은 1~9 구간 제약이 느슨한데, 2안은 반대로 1~9 구간을 최소 1개 강제.

```
odd_even 1 5               # 홀수 개수 1~5개 허용 (1안과 동일 — 차별화 요소 아님)
sum 88 189                 # 6개 번호 합계 88~189 (1안과 동일)
consecutive_max 3          # 연속번호 최대 3개까지 (1안과 동일)
zone 1 9 min 1 max 3       # 1~9번대는 최소 1개 (1안: 0~3개, 반대로 강제 포함)
zone 10 19 min 0 max 3     # 10~19번대는 0~3개 (1안과 동일)
zone 20 29 min 0 max 3     # 20~29번대는 0~3개 (1안과 동일)
zone 30 39 min 0 max 3     # 30~39번대는 0~3개 (1안과 동일)
zone 40 45 min 0 max 0     # 40~45번대는 완전 배제 (1안: 1~2개 강제, 정반대)
predictions 5              # 예측 세트 5개 생성
freq_all_weight 0.4        # 전체 출현 빈도 반영 비중 40%
freq_recent_weight 0.6     # 최근 회차 출현 빈도 반영 비중 60%
freq_recent_window 20      # "최근"의 기준 = 최근 20회차
freq_mode cold             # 1안(hot)과 반대로 "최근 안 나온 번호" 우대
```

## 실행 방법

```
py scripts/predict.py --rules RULES_2.md --out-prefix predictions2
```

`predictions/round_<n>/predictions2_<n>_<타임스탬프>.json`으로 저장되며,
1안 파일명(`predictions_...json`)과 접두사가 달라 대시보드/리포트 스크립트가
1안만 인식하는 기존 동작에는 영향을 주지 않습니다 (2안을 대시보드에 반영하는
작업은 별도로 진행 예정).

## 조건 종류 설명

RULES.md와 동일한 문법입니다. 자세한 키워드 설명은 `RULES.md`의 "조건 종류 설명" 표를 참고하세요.
2안에서만 쓰는 키워드:

| 키워드 | 형식 | 의미 |
|---|---|---|
| `freq_mode` | `freq_mode hot|cold` | `hot`=최근 자주 나온 번호 우대(기본값, RULES.md와 동일), `cold`=최근 안 나온 번호 우대 |
